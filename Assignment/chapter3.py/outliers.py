   """
mean is the most affected by outliers. the mean uses every value in the calculation .

median depend only on the position of the middle value after sorting. so outlier stay at the end and rarely change the middle value. so median is not an outlier.

mode depend only on frequency. an outlier ususall values.

"""
